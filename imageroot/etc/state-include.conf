#
# Restic include patterns for the ClamAV module backup (paths relative to /srv).
# The signature database (volume clamav-db) is deliberately not included: it is
# re-downloaded on the first start after a restore.
#
state/environment
state/clamav.env
